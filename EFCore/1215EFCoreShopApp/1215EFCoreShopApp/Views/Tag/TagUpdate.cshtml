@model Tag

@using (Html.BeginForm("TagUpdate", "Tag", FormMethod.Post))
{
    @Html.LabelFor(a => a.Name)
    @Html.TextBoxFor(a => a.Name)
    @Html.ValidationMessageFor(a => a.Name)
    <br />

    <input type="submit" value="Submit" />
}
